---
- name: Apache insallation
  hosts: node2
  remote_user: vagrant
  become: yes


  tasks:
        - name: Apache latest version installation
          yum:
                pkg: httpd
                state: absent

